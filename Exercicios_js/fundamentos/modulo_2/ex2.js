/* 
  escreva funçaõ que receba dois  @param{number}
  - se forem numeros iguais retorn esse numero
*/

function compareNum(a, b) {
  let compare = a > b ? a : b
  return compare
}

console.log(compareNum(15, 8))
